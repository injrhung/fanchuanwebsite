# 小帆船靜態網站

零依賴單頁靜態網站，直接開啟 `index.html` 即可瀏覽，也可執行 `npm run dev` 在本機預覽。正式輸出使用 `npm run build`，檔案會產生於 `dist/`。

目前餐點圖片暫時引用舊站公開素材。取得新圖片後，請依 HTML 中的 `data-asset-role` 對應替換並改為站內資產。價格、配送、營業時間、社群連結與品牌說法仍應由店家定期確認。

正式圖片請放在 `images/` 資料夾，建置時會自動複製到 `dist/images/`。
